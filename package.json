{
  "name": "react-tunes",
  "version": "1.0.0",
  "description": "React Tunes - Music Streaming App",
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd server && npm install && cd ../client && npm install && npm run build",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 